# Sample Output

Run the framework against a domain you are authorized to assess and copy the generated `reports/output/` files here if required by the internship submission.

The generated report contains DNS, HTTP, TLS, RDAP, robots/sitemap and basic security-header observations.
