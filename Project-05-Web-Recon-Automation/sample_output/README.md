# Sample Output

Run the framework against a domain you are authorized to assess and copy the generated `reports/output/` files.

The generated report contains DNS, HTTP, TLS, RDAP, robots/sitemap and basic security-header observations.
